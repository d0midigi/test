# 3.12.6 Network Conditions That Trigger Authentication Fallback

_Name-resolution failures, SPN problems, service configuration, legacy dependencies, or application behavior may cause environments to fall back from Kerberos to NTLM._
