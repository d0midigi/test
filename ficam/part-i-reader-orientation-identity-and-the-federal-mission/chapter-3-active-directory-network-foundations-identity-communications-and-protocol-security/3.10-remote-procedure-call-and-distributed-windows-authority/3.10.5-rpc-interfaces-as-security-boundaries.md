# 3.10.5 RPC Interfaces as Security Boundaries

_Individual RPC interfaces expose different operations and authorization requirements, meaning that network reachability to RPC does not imply identical access to every function._
