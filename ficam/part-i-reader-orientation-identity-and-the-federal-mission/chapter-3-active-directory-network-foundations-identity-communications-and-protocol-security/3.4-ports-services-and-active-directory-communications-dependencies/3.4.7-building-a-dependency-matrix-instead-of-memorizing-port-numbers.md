# 3.4.7 Building a Dependency Matrix Instead of Memorizing Port Numbers

_The operational objective is not memorization. Practitioners must understand which identity function requires a communication path, which systems participate in it, what security property the protocol provides, and what breaks when the path is altered or blocked._
